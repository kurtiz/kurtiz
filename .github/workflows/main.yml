name: Update badges

on:
  schedule:
    # Runs at 11:31 UTC
    - cron: "31 11 * * *"
jobs:
  update-readme:
    name: Update Readme with badges
    runs-on: ubuntu-latest
    steps:
      - name: Badges - Readme
        uses: kurtiz/kurtiz@master
          CREDLY_USER: aaron-will-djaba 
          # optional, but default will use the same from github
